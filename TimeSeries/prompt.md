你现在将为一个时间序列预测项目生成完整的文档、代码与说明文件结构。  
请严格按以下要求生成内容：

============================================================
【项目背景与需求】
我们正在进行一个关于“基于热电芯片天空辐射采集与分析”的实验项目，方法就是利用八个相同的热电芯片加上不同波段范围的滤光片，在同一太阳灶的焦点位置，测量不同波段的辐射强度。

我有一个物理实验得到的数据集，其结构如下：
- 每次实验采集一段连续数据（例如 12:00~17:00），采样间隔固定为 10 秒(实际保存到csv文件中可能是9s-11s，但是无关紧要，当作10s处理即可)；
- 不同日期之间的数据段彼此完全不连续；（可以参照TimeSeries/1122.csv和TimeSeries/1129.csv，间隔了七天，并且每天的测量时间段也有所不同，但是都是10s测一组数据）
  之后我们会测量更多数据，时间段基本都是上午10:00到下午16:00，不会超过这个范围，例如这样的示例：
  * 11月22日，12:00~17:00，每隔十秒钟，八个电压数据
  * 11月29日，15:00~16:00，每隔十秒钟，八个电压数据
  * 12月05日，13:00~16:00，每隔十秒钟，八个电压数据
- 每条记录包含 8 个通道的电压值，以及时间戳；
- 数据不能进行插值（不能人工补点）；
- 模型训练必须在本地 PC（PyTorch + RTX 4060 GPU）约 30 分钟以内完成。

因此，需要基于 PyTorch 构建一个可训练 LSTM/GRU 的时间序列预测模型，其中每一天的数据是一条独立的序列 (segment)，模型不应该跨日期连接序列。

============================================================
【你需要生成的内容】

## 1. 《时间序列基础 & LSTM/GRU 入门》文档（Markdown）
请生成一份易读、详细、入门友好的介绍文档，内容包括：
- 时间序列数据（特别是不等间隔片段组成的数据集）的基本概念
- 片段式时间序列（Segment-based sequence modeling）的概念
- RNN/LSTM/GRU 的核心思想、适用场景、输入输出维度解释
- 在不插值条件下构建模型的注意事项
- 实验数据如何打包成模型可接受的 batch

文件名：`TimeSeries/docs/time_series_intro.md`

## 2. 完整的 PyTorch 模型训练代码脚本
请生成以下 Python 脚本，每个脚本需结构清晰、注释完整、可直接运行。
我们不强求模型预测准确性，因此你可以给出15min，30min，60min内训练完成的配置说明。
### (1) 数据加载脚本
文件：`TimeSeries/src/dataset.py`
内容要求：
- 支持读取若干个独立片段序列（Segment）
- 每段序列形状为 `[seq_len, 8]`
- 自动按天划分
- 构建 PyTorch Dataset / DataLoader
- 不使用插值

### (2) GRU 模型脚本
文件：`TimeSeries/src/model_gru.py`
内容要求：
- 使用 PyTorch nn.GRU
- 包含输入维度、隐藏层、层数等可配置参数
- forward() 清晰易读，适合时间序列预测

### (3) LSTM 模型脚本
文件：`TimeSeries/src/model_lstm.py`
内容要求：
- 使用 PyTorch nn.LSTM
- 与 GRU 版本功能一致，便于替换模块

### (4) 训练脚本
文件：`TimeSeries/src/train.py`
内容要求：
- 使用 GPU（自动检测 CUDA）
- 训练不超过 30 分钟（最好不要超过15min）的合理设置（如 batch_size、hidden_size）
- Loss函数按照你认为最合适的选即可
- 包含：
  - 训练循环
  - 验证集评估
  - 模型保存与日志输出
- 可在命令行传入参数：`--model gru/lstm`

### (5) 推理脚本（预测）
文件：`TimeSeries/src/predict.py`
内容要求：
- 加载模型
- 输入一段序列，生成未来若干步的预测值
- 输出预测数组

============================================================
## 3. 每个 Python 文件对应一个解释文档（Markdown）
例如：
- `TimeSeries/docs/dataset.md` — 解释 dataset.py 的核心逻辑
- `TimeSeries/docs/model_gru.md` — 解释 GRU 代码关键段落
- `TimeSeries/docs/model_lstm.md`
- `TimeSeries/docs/train.md`
- `TimeSeries/docs/predict.md`
格式要求：
- 段落清晰
- 用简短示意图解释维度变化
- 标明每一段代码的功能

============================================================
## 4. 项目文件结构说明（Markdown）
文件：`TimeSeries/docs/structure.md`
内容：
- 项目总目录树
- 每个 main 文件的用途
- 推荐的训练流程
- 如何添加新数据

============================================================
## 5. 项目 README.md
请生成一个清晰、专业且易读的 README，放在`TimeSeries/`中包括：
- 项目介绍
- 数据格式说明
- 如何准备数据
- 如何训练模型
- 如何切换 GRU / LSTM
- 如何进行预测
- 环境需求（CUDA / PyTorch 版本等）

============================================================

请开始生成上述全部内容。
